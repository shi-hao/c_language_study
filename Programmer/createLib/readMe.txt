动态连接和静态连接的区别：
动态库不会编译进入可执行文件，可执行文件在运行的时候会在运行环境下动态加载库。
静态连接会直接将库打包进入可执行文件。

C程序编译和运行
链接库是在编程开发中很常见的事情，关于链接库搞清楚以下几点：
（1）gcc编译时，连接库的用法
-l 指定库名
-L 指定库的路径

（2）程序运行时，连接库
程序在执行时，需要动态加载库，会在系统库目录下查找在编译时连接的库。
linux下的工具 ldd 可以很好查看可执行文件在运行时需要的库。
ldd 可执行文件
